package strings;

public class NumberToString {
    public static void main(String[] args) {

        NumberToString stringToNumber = new NumberToString();

        int first = stringToNumber.convertNumberToString(123);
        int second = stringToNumber.convertNumberToString(-123);

        System.out.println("Finished converting string to number: " + first + ";" + second);
    }
    /*
     * Having an int return the string representation
     * If the int is 123 return "123" if the int is -123 return "-123"
     */
    public int convertNumberToString(int value){
return 0;
    }
}
